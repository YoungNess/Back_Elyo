package elyo.back.repository;

import elyo.back.model.DetailSae;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DetailSaeRepository extends JpaRepository<DetailSae, Long> {
}